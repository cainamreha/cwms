    	<article class="listEntry {t_class:thirdrow} {altClass}">
			<div class="{t_class:panel} flex-col">
				<header class="{t_class:panelhead}">
					{dataLink}
						<h1 class="dataHeader {t_class:paneltitle}">{dataHeader}</h1>
					</a>
				</header>
				<div class="{t_class:panelbody} flex-grow">
					{teaserImg}
					<div class="dataTeaser">{dataTeaser}</div>
				</div>
				<div class="{t_class:panelfoot}">
					{dataLink}
						<span class="readMoreSpan">
							<span class="readMore {t_class:btn} {t_class:btndef} {t_class:btnblock}">{more}</span>
						</span>
					</a>
				</div>
			</div>
        </article>